# 快准车服查询 Agent 接口文档

本文档集面向本项目的“快准车服查询 Agent”。采集时间为 2026-08-12，证据来自已登录的快准车服站管家 v2.0 页面及页面正常查询操作产生的网络请求。

## 文档导航

- [采集方法与安全边界](./methodology.md)
- [查询入口覆盖矩阵](./catalog.md)
- [共享辅助查询接口](./shared.md)
- [商品与品牌查询接口](./products.md)
- [库存查询接口](./inventory.md)
- [销售查询接口](./sales.md)
- [财务查询接口](./finance.md)

`raw/` 目录是仓库原有的早期单接口脱敏笔记，未在本轮改动。上面的领域文档是本轮审计后的规范入口；当两者默认参数不同时，以领域文档记录的当前页面实测值为准。

## 使用约定

- 文档中的路径均相对于快准车服基地址 `https://dgj8.kzmall.cc/index.php`；本项目通过 `/api/*` 薄代理调用时去掉该基地址并在前面加 `/api`。
- 页面会话使用 Cookie 鉴权。不要把 Cookie、Token、用户信息或真实业务记录写入日志、提示词或持久化文档。
- 在本项目中，浏览器把现有登录 Cookie 发送给 `/api/*`，Worker 透传 Cookie 并统一注入请求头 `sun: 5516`。前端 Agent 不应自行读取 Cookie，也不应在业务请求里硬编码或展示认证信息。
- `nd` 是页面生成的时间戳型防缓存参数，不属于业务过滤条件。
- jqGrid 风格接口常用 `_search`、`rows`、`page`、`sidx`、`sord` 处理搜索、分页和排序。
- 页面虽可能使用 `POST` 执行查询，仍应按“只读查询”对待；不得把路径或参数改造成保存、编辑、删除、审核、导入、调拨、开单等动作。

## 证据等级

| 等级 | 含义 |
|---|---|
| A | 通过页面正常打开/查询抓到真实请求，并读取到 JSON 响应字段结构 |
| B | 抓到真实请求和 UI 出入参，但响应无数据或载荷为站点编码字符串，无法在不分析 JS bundle 的前提下提取字段结构 |
| C | 仅确认页面入口、查询控件或结果列；主接口请求未被正常抓包证明 |
| X | 非查询入口，或属于会改变业务状态的流程，按安全要求未进入/未操作 |

只有 A/B 级接口可直接进入 Agent 的 HTTP 工具清单。C 级页面只能用于描述产品能力，获得后端正式契约或正常页面请求证据前不得生成可执行调用。
