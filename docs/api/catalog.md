# 查询入口覆盖矩阵

状态说明：`A` 表示真实查询请求、非空响应字段和 UI 逐项映射均已确认；`B` 表示真实请求和模板/组件字段契约已确认，但结果为空或类型/可空性仍不完整；`FR` 表示已抓到会话化 FineReport 参数/取页协议，但它不是稳定业务 REST；`C` 表示仅有入口/UI 或 bundle 契约，主查询未通过正常条件实际触发，不得生成 HTTP 工具；`X` 表示会改变业务状态的流程，按安全边界排除。`辅助` 表示非独立菜单页的下拉/树查询。

## 商品与品牌

| 菜单/能力 | 状态 | 说明 |
|---|---|---|
| 商品管理 | 已取证 A | 快准商品请求、正式 `kziv` 解码调用、非空逻辑 schema 和列映射已确认 |
| 三方商品管理 | 已取证 A | 三方商品请求、正式解码后的非空 schema 和列映射已确认 |
| 套包管理 | 已取证 A | 套包分页查询；组装/拆包操作排除 |
| 报价规则管理 | 已取证 A | 规则分页列表；新增、设置、编辑、复制、删除排除 |
| 品牌列表 | 已取证 A | 查询页自动调用的品牌辅助接口 |
| 快准分类树 | 已取证 A | 销售/库存/商品查询辅助接口 |
| 三方分类/业务类别 | 已取证 A | 查询页自动调用的辅助接口 |

## 库存

| 菜单 | 状态 | 说明 |
|---|---|---|
| 库存查询 | 已取证 A | 库存余额查询 |
| 库存盘点 | 排除 X | 盘点业务流程，会改变库存状态 |
| 三方库存盘点 | 排除 X | 盘点业务流程 |
| 货位调整 | 排除 X | 调整业务流程 |
| 货位调整查询 | 已取证 A | 调整记录查询 |
| 库存调拨单 | 排除 X | 调拨制单流程 |
| 库存调拨单查询 | 已取证 B | 列表与状态计数；当前默认条件下列表为空 |
| 其它入库单 | 排除 X | 入库制单流程 |
| 其它入库单查询 | 已取证 A | 入库单列表查询 |
| 其它出库单 | 排除 X | 出库制单流程 |
| 其它出库单查询 | 已取证 B | 查询请求已确认；当前默认条件下列表为空 |
| 商品收发明细表 | 已取证 B | 请求、条件、结果列已确认；大响应结构不完整 |
| 商品收发汇总表 | 已取证 A | 请求与分组结果列已确认 |

## 销售

| 菜单 | 状态 | 说明 |
|---|---|---|
| 商品报价 | 排除 X | 报价创建流程 |
| 快速报价 | 排除 X | 报价创建流程 |
| 报价单管理 | 已取证 A/B | 有效报价 A；等待/作废报价请求与列契约已确认但当前为空 |
| 销售单 | 排除 X | 销售开单流程 |
| 销售单管理 | 已取证 A | `getSalesOrderlist` 列表、待出库计数、筛选参数和行字段映射已确认 |
| 出库单管理 | 已取证 A | `outboundList` POST、非空响应、来源枚举及全部主表列已确认 |
| 销售退货单 | 排除 X | 退货制单流程 |
| 销售退货单管理 | 已取证 A | GET 列表请求、非空响应和 jqGrid 字段已确认 |
| 微仓备货推荐 | 已取证 C | bundle 已确认 `firstMatch/recommend` 完整分支参数、响应容器和列绑定；未选择真实客户触发 |
| 微仓补货管理 | 已取证 C | bundle 已确认主查询与补货推荐的参数、响应容器和列绑定；未选择真实客户触发 |
| 微仓出入库查询 | 已取证 B | 参数、空响应容器、8 个单据类型和 11 列字段已确认 |
| 微仓库存管理 | 已取证 C | bundle 已确认 `getMoveStorageInventory` 完整参数、响应容器和列绑定；未选择真实微仓触发 |
| 微仓账号管理 | 已取证 B | 空请求/响应、`mobile/name/status` 列契约已确认，不保留身份数据 |
| E站套餐管理 | 已取证 B | 请求体、响应外层和组件列字段已确认；当前列表为空，字段类型待补证 |
| E站活动管理 | 已取证 A | POST 请求体与行 schema 已确认 |
| E站积分商品 | 已取证 A | 请求体、分类枚举、非空响应和列字段映射已确认 |
| E站订单管理 | 已取证 A | 请求默认值、可选筛选项、非空响应和列字段映射已确认 |
| E站积分订单 | 已取证 A | 默认日期、请求体、非空响应和列字段映射已确认 |
| E站退货申请 | 已取证 A | GET 列表、状态枚举、非空响应和列字段映射已确认 |
| E站关单退款 | 已取证 A | JSON 请求体、枚举、非空响应和列字段映射已确认 |
| 商品调拨单 | 排除 X | 调拨制单流程 |
| 商品调拨单管理 | 已取证 A | `stf/apply/list` POST、非空响应、状态枚举和列映射已确认 |
| 大客户销售出库单 | 排除 X | 大客户开单流程 |
| 大客户销售出库单查询 | 已取证 A | 当前非空请求/响应、完整筛选参数、状态枚举和全部列映射已确认 |
| 大客户销售退货单 | 排除 X | 退货制单流程 |
| 大客户销售退货单查询 | 已取证 B | `invCu?action=list` 全部参数与列绑定已确认，当前响应为空 |
| 大客户销售配送明细表 | 已取证 A | `getInitCuSale_detail`、非空响应类型及列映射已确认 |
| 大客户销售退货明细表 | 已取证 B | 同端点 `transType=180602`，当前列表为空 |
| 大客户平台订单 | 已取证 A | JSON 请求、筛选枚举、1 行非空响应和全部列已确认 |
| 大客户平台退单 | 已取证 B | JSON 请求、筛选枚举、空响应外层和全部列已确认 |
| 销售明细表 | 已取证 A | 普通客户商品销售明细 |
| 销售汇总表 | 已取证 A | 按商品 `data.rows[]`、按客户 `data.list[]` 及动态仓库列已确认 |
| 销售对账明细表 | 已取证 A | `getSaleBalance`、动态支付列、非空响应及枚举已确认 |

## 财务

| 菜单 | 状态 | 说明 |
|---|---|---|
| 收款单 | 排除 X | 收款制单流程 |
| 收款单管理 | 已取证 A | POST 请求体、非空响应和表格 `prop` 映射已确认 |
| 客户账户管理 | 已取证 A | POST 列表/统计、非空响应和表格 `prop` 映射已确认 |
| 付款单 | 排除 X | 付款制单流程 |
| 付款单查询 | 已取证 A | 参数、33 行非空响应、分页容器和可见列已确认 |
| 调拨收付款单 | 排除 X | 制单流程 |
| 调拨收付款单查询 | 已取证 B | 参数与列已确认；扩大日期范围后仍为空，类型/可空性待补证 |
| 服务站账户管理 | 已取证 A | 两个空 JSON POST、非空账户/待调账响应及页面指标映射已确认 |
| 线上流水查询 | 已取证 B | POST 请求体、响应外层和组件列映射已确认；当前列表为空，字段类型待补证 |
| 三方成本调整单 | 排除 X | 成本调整流程 |
| 三方成本调整单查询 | 已取证 A | 完整筛选枚举、3 行非空响应和列绑定已确认 |
| 其它收入单 | 排除 X | 收入制单流程 |
| 其它收入单查询 | 已取证 A | 参数、3 行非空响应、分页容器和列绑定已确认 |
| 其它支出单 | 排除 X | 支出制单流程 |
| 其它支出单查询 | 已取证 A | 供应商参数、3 行非空响应、分页容器和列绑定已确认 |
| 经营报表 | 已取证 A | 日/月接口、非空响应、日期参数、汇总对象和全部主表列已确认 |
| 利润表 | 已取证 A | 9 行非空响应、嵌套主营/其他业务数组和前端派生字段已确认 |
| 现金银行报表 | 已取证 A | 908 行非空流水、页脚汇总和全部列已确认 |
| 应付账款明细表 | 已取证 A | 829 行非空响应、供应商参数、汇总和列绑定已确认 |
| 应收账款总表 | 已取证 FR | FineReport 参数提交、取页协议和渲染列已确认；不生成普通 HTTP 工具 |
| 应收账款明细表 | 已取证 FR | FineReport 会话协议、参数和列标题已确认；旧版字段绑定仅作逻辑释义 |
| 客户对账单 | 已取证 C | bundle 已确认 `customerBalance_detail` 参数、业务类别枚举和列字段；未代选真实客户触发主查询 |
| 供应商对账单 | 已取证 C | bundle 已确认 `supplierBalance_detail` 参数和列字段；未代选真实供应商触发主查询 |
| 客户应收余额表 | 已取证 A | `getCustomerBalance` 非空响应、动态支付列和筛选枚举已确认 |
