# 查询入口覆盖矩阵

状态说明：`A` 表示正常页面查询、请求契约与响应字段结构均已确认；`B` 表示真实请求和 UI 契约已确认，但结果为空、编码载荷或明文 schema 不完整；`C` 表示仅有入口/UI 契约，主查询请求未被正常抓包证明，不得生成 HTTP 工具；`X` 表示会改变业务状态的流程，按安全边界排除。`辅助` 表示非独立菜单页的下拉/树查询。

## 商品与品牌

| 菜单/能力 | 状态 | 说明 |
|---|---|---|
| 商品管理 | 已取证 B | 快准商品列表与综合搜索；响应 `data` 为编码字符串 |
| 三方商品管理 | 已取证 B | 三方商品列表与综合搜索；响应 `data` 为编码字符串 |
| 套包管理 | 已取证 A | 套包分页查询；组装/拆包操作排除 |
| 报价规则管理 | 已取证 A | 规则分页列表；新增、设置、编辑、复制、删除排除 |
| 品牌列表 | 已取证 A | 查询页自动调用的品牌辅助接口 |
| 快准分类树 | 已取证 A | 销售/库存/商品查询辅助接口 |
| 三方分类/业务类别 | 已取证 A | 查询页自动调用的辅助接口 |

## 库存

| 菜单 | 状态 | 说明 |
|---|---|---|
| 库存查询 | 已取证 A | 库存余额查询 |
| 库存盘点 | 排除 X | 盘点业务流程，会改变库存状态 |
| 三方库存盘点 | 排除 X | 盘点业务流程 |
| 货位调整 | 排除 X | 调整业务流程 |
| 货位调整查询 | 已取证 A | 调整记录查询 |
| 库存调拨单 | 排除 X | 调拨制单流程 |
| 库存调拨单查询 | 已取证 B | 列表与状态计数；当前默认条件下列表为空 |
| 其它入库单 | 排除 X | 入库制单流程 |
| 其它入库单查询 | 已取证 A | 入库单列表查询 |
| 其它出库单 | 排除 X | 出库制单流程 |
| 其它出库单查询 | 已取证 B | 查询请求已确认；当前默认条件下列表为空 |
| 商品收发明细表 | 已取证 B | 请求、条件、结果列已确认；大响应结构不完整 |
| 商品收发汇总表 | 已取证 A | 请求与分组结果列已确认 |

## 销售

| 菜单 | 状态 | 说明 |
|---|---|---|
| 商品报价 | 排除 X | 报价创建流程 |
| 快速报价 | 排除 X | 报价创建流程 |
| 报价单管理 | 已取证 C | 入口与 UI 契约已确认，主列表路径未暴露 |
| 销售单 | 排除 X | 销售开单流程 |
| 销售单管理 | 已取证 B | GET 列表和 POST 待出库计数已确认 |
| 出库单管理 | 已取证 C | 入口、条件和字段已确认，主列表路径未暴露 |
| 销售退货单 | 排除 X | 退货制单流程 |
| 销售退货单管理 | 已取证 B | GET 列表请求和 UI 字段已确认 |
| 微仓备货推荐 | 已取证 C | 完整 UI 契约，主请求路径未暴露 |
| 微仓补货管理 | 已取证 C | 完整 UI 契约，主请求路径未暴露 |
| 微仓出入库查询 | 已取证 B | POST 列表及单据类型辅助接口 |
| 微仓库存管理 | 已取证 C | 完整 UI 契约，主请求路径未暴露 |
| 微仓账号管理 | 已取证 B | POST 列表路径与输出列已确认，不保留身份数据 |
| E站套餐管理 | 已取证 A | POST 请求体与响应容器已确认 |
| E站活动管理 | 已取证 A | POST 请求体与行 schema 已确认 |
| E站积分商品 | 已取证 B | POST 路径、条件与 UI 字段已确认 |
| E站订单管理 | 已取证 B | POST 路径和请求体键已确认 |
| E站积分订单 | 已取证 B | POST 路径和请求体键已确认 |
| E站退货申请 | 已取证 B | GET 列表与客户辅助查询已确认 |
| E站关单退款 | 已取证 B | POST 列表路径和请求体键已确认 |
| 商品调拨单 | 排除 X | 调拨制单流程 |
| 商品调拨单管理 | 已取证 C | 入口、条件和字段已确认，主列表路径未暴露 |
| 大客户销售出库单 | 排除 X | 大客户开单流程 |
| 大客户销售出库单查询 | 已取证 A | 仓库脱敏请求笔记与当前 UI 入口已复核 |
| 大客户销售退货单 | 排除 X | 退货制单流程 |
| 大客户销售退货单查询 | 已取证 C | 入口、条件与字段已确认，主请求未暴露 |
| 大客户销售配送明细表 | 已取证 C | UI 契约已确认，报表主请求未暴露 |
| 大客户销售退货明细表 | 已取证 C | UI 契约已确认，报表主请求未暴露 |
| 大客户平台订单 | 已取证 B | POST 列表路径和全部请求体键已确认 |
| 大客户平台退单 | 已取证 B | POST 列表路径和全部请求体键已确认 |
| 销售明细表 | 已取证 A | 普通客户商品销售明细 |
| 销售汇总表 | 已取证 C | 入口与按商品/客户 UI 契约已确认，主请求未暴露 |
| 销售对账明细表 | 已取证 C | 入口、条件与字段已确认，主请求未暴露 |

## 财务

| 菜单 | 状态 | 说明 |
|---|---|---|
| 收款单 | 排除 X | 收款制单流程 |
| 收款单管理 | 已取证 B | POST 列表请求体键、汇总路径及 UI 契约已确认 |
| 客户账户管理 | 已取证 B | POST 列表/统计路径及 UI 契约已确认 |
| 付款单 | 排除 X | 付款制单流程 |
| 付款单查询 | 已取证 A | 付款单列表 |
| 调拨收付款单 | 排除 X | 制单流程 |
| 调拨收付款单查询 | 已取证 A | 调拨收/付款列表 |
| 服务站账户管理 | 已取证 B | 自动加载账户汇总与白条待调账列表 |
| 线上流水查询 | 已取证 B | POST 路径、全部请求体键与 UI 契约已确认 |
| 三方成本调整单 | 排除 X | 成本调整流程 |
| 三方成本调整单查询 | 已取证 A | 成本调整单列表 |
| 其它收入单 | 排除 X | 收入制单流程 |
| 其它收入单查询 | 已取证 A | 其它收入单列表 |
| 其它支出单 | 排除 X | 支出制单流程 |
| 其它支出单查询 | 已取证 A | 其它支出单列表 |
| 经营报表 | 已取证 B | 日经营查询路径已确认，动态字段待 schema |
| 利润表 | 已取证 A | 月度主营、其他业务与合计利润 |
| 现金银行报表 | 已取证 A | 账户流水、收入支出与余额 |
| 应付账款明细表 | 已取证 A | 供应商应付、付款与余额 |
| 应收账款总表 | 已取证 C | UI 契约已确认；隔离报表的业务路径未暴露 |
| 应收账款明细表 | 已取证 C | UI 契约已确认；隔离报表的业务路径未暴露 |
| 客户对账单 | 已取证 C | 入口、条件与字段已确认；需选择客户才会发主请求 |
| 供应商对账单 | 已取证 C | 入口、条件与字段已确认；需选择供应商才会发主请求 |
| 客户应收余额表 | 已取证 C | UI 契约和字段已确认；业务路径未暴露 |
